

### Completed: Implement player and ball classes with vector movement and boundaries (entities.py)
Implemented entities.py with Ball and Player classes (both pygame.sprite.Sprite subclasses), plus a new config.py module as the single source of truth for all shared constants (breaking the circular import between main.py and entities.py). Ball: position/velocity as pygame.math.Vector2, radius=10, damping=0.999, update(dt) integrates position and bounces off all four pitch edges (velocity inversion + clamp), reset() recenters. Player: position/velocity/direction as Vector2, radius=15, speed=300 px/s, set_direction(dx,dy) normalizes input, update(dt) moves and clamps at pitch boundaries (no bounce, zeroes out-of-bounds velocity component). Integration: Game.__init__ instantiates both entities; Game.update(dt) calls their update methods; Game.draw(surface) renders them after the field; Game.poll_input() handles WASD/arrow keys. Architecture: Clean DAG - config.py (leaf) feeds entities.py feeds main.py. No circular imports. QA: PASS (circular import resolved, all lint issues fixed, headless-safe). Tests: 22/22 pass (17 in test_main.py + 5 in test_entities.py), including subprocess import-isolation test, wall-bounce/clamp verification, direction normalization, and OpenCV visual field check.

### Completed: Implement physics collision handling, goal detection, and score tracking logic (physics.py)
Implemented and verified physics.py with Score class (p1/p2 tracking, goal_for(), draw(surface) with cached font) and PhysicsEngine class (circle-circle collision with separation/velocity reflection/momentum transfer/speed clamping at 800px/s, left/right goal detection with score awarding and ball reset). Fixed critical goal-detection bug: entities.py Ball.update() now skips left/right wall bounce when ball.y is within goal mouth (WINDOW_HEIGHT/2 ± GOAL_BOX_HEIGHT/2), making the goal mouth open. Fixed Score.draw() to not require external font param. Fixed lint issue (unnecessary quoted annotation). QA: PASS (all checks green, lint clean, headless-safe, no circular imports, correct collision math, goal detection reachable). Tests: 12/12 test_physics.py pass (goal-mouth pass-through, wall bounce, collision, scoring, reset); 27 total pass across all test files (7 pre-existing env errors from unavailable dummy video driver in Windows pygame build, unrelated to code logic).

### Completed: Build automated test script using pytest and OpenCV visual verification (test_game.py)
Created test_game.py — a 12-test pytest + OpenCV integration suite for the Pygame Soccer game. Covers: TestGameIntegration (initialization, 10-frame update/draw cycle, ball bounce bounds), TestVisualVerification (green field >50% via cv2.inRange, white lines, ball/player visibility via cv2.HoughCircles, frame dimensions 600x800x3, no black holes), TestPhysicsIntegration (ball-player collision, goal detection, speed clamp). Headless-safe (SDL_VIDEODRIVER=dummy), deterministic, no external data files. QA audit caught a pytest.approx misuse in test_ball_bounce (fixed with explicit tolerance) and 13 lint findings (all resolved). Final result: 12/12 tests pass, ruff clean.